spring:
  application:
    name: allinone
  security:
    oauth2:
      resourceserver:
        jwt:
# ${wiremock.server.port} is automatically injected by the WireMock Spring integration.
          jwk-set-uri: http://localhost:8787/.well-known/jwks.json
  r2dbc:
    url: r2dbc:tc:postgresql:///databasename?TC_IMAGE_TAG=16